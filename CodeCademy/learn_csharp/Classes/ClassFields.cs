using System;

namespace LearnCsharp
{
    public class ClassFields
    {
        // ? Class members:
        // *    - fields
        // *    - properties
        // *    - methods
        // *    - constructors

        // !  - Fields (It is common practice to names field with lowercase (name instead of Name). This makes fields easy to recognize later on.)
        public string name;
        public int trees;
        // ? We haven’t set the value of either field, so each has a default value. In this case:
        // *    - strings default to null,
        // *    - ints to 0,
        // *    - and bools to false.
    }
}

